{
    email victor.olivares@emma.pe
}

descubre.emma.pe {
    encode gzip

    # Manejo de API y carga de archivos
    handle /api/v1* {
        reverse_proxy backend:3001
    }

    handle /uploads* {
        reverse_proxy backend:3001
    }

    # Frontend (Next.js)
    handle {
        reverse_proxy frontend:3000
    }
}